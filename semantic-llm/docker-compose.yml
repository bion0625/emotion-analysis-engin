# 모델 다운 명령어

# window gpu 설치 나중에 설정
# 리눅스 gpu 설치도 나중에 설정

# docker compose up -d
# docker compose exec ollama ollama pull llama3:8b

version: '3.8'

services:
  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data: